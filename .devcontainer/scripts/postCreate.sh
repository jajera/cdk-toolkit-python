#!/bin/bash

set -e # Exit immediately if a command exits with a non-zero status

# Display system information
echo "System Information:"
uname -a

# python -m venv .venv

# source .venv/bin/activate

# pip install -r src/hello_api/requirements.txt
